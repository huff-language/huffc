#define macro FIRST_LOCATION = takes(0) returns(1) {
    0x00
}

#define macro SECOND_LOCATION = takes (0) returns (1) {
    0x00
}

#define macro CONSTRUCTOR = takes (0) returns (1) {
    FIRST_LOCATION()
}

#define macro MAIN = takes (0) returns (1) {
    SECOND_LOCATION()
}