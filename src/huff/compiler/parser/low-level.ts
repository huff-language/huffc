/**
 * @param macro The name of the macro
 * @param macros Maps macros to their names, args, and raw code data
 * @returns An array containing the macro's parsed code
 */
const parseMacro = (macro: string, macros: {}): any[] => {
  return [];
};
